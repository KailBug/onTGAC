# Text2SQL Agent Solution - 胡辣汤

---
* **模型选择：** qwen3-coder-480b-a35b-instruct、text-embedding-v4、kimi-k2-thinking-turbo
* **参数设置：** Temperature = 0.1, Top-p = 5
* **OS:** Windows 11
* **Python:** 3.13.9
* **Database:** starrocks/allin1-ubuntu:2.5.12 with Docker Desktop
* **显存要求:** 非本地计算
* **项目依赖:** requirement.txt

---
```python
if __name__ == '__main__':

    #模式变量 INSERT、 EXECUTE、 OTHER
    MODE  = ""

    sql_executor = execute_sql_with_pymysql()
    db_configuration = {
        'host': 'localhost',                    # 数据库主机地址
        'user': 'root',                         # 数据库用户名
        'password': '',                         # 数据库密码
        'db': 'kail0',                          # 数据库名称_final_algorithm_competition
        'port': 9030                            # starrocks访问端口
    }

```
#### INSERT模式，执行主办方提供的数据库初始化工作，建表和插入数据
#### EXECUTE模式，将final_dataset_with_mapping.json中生成的SQL连接到数据库执行，生成dataset_exe_result.json
#### OTHER模式，用于开发Agent阶段的测试，包括将问题生成对应的SQL，并存储到final_dataset_with_mapping.json